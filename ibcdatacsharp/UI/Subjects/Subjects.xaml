<Page x:Class="ibcdatacsharp.UI.Subjects.Subjects"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ibcdatacsharp.UI.Subjects"
      xmlns:controls="clr-namespace:System.Windows.Controls;assembly=MultiSelectTreeView"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Subjects">

    <controls:MultiSelectTreeView BorderThickness="0" x:Name="treeView" ItemsSource="{Binding subjectsList}">
        <controls:MultiSelectTreeView.ItemTemplate>
            <HierarchicalDataTemplate DataType="{x:Type local:Subject}">
                <controls:MultiSelectTreeViewItem Header="{Binding nombre}">
                    <controls:MultiSelectTreeViewItem Header="{Binding centroMedico}"/>
                </controls:MultiSelectTreeViewItem>
            </HierarchicalDataTemplate>
        </controls:MultiSelectTreeView.ItemTemplate>
    </controls:MultiSelectTreeView>
</Page>
